# Polyglot native mySQL DB on localhost, this app running natively
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/polyglot

# Database credentials
spring.datasource.username=polyglot
spring.datasource.password=loh8eeTooj0i

# Can be tested with: mysql -h 127.0.0.1 -P 3306 --protocol=tcp -u polyglot -p

# Connector setitngs, specifically fpor mysql5
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
